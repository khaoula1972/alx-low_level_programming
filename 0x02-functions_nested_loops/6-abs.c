#include "main.h"
/**
 * _abs - Entry point
 * @n: interger
 * Description : 'Absolute value of a number'
 * Return: the absolute value
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}
